# Architecture diagrams

System, auth, ERP integration, and shift workflow diagrams live in the root [README.md](../../README.md#architecture) as markdown code blocks for GitHub-native rendering.

Optional exports (SVG/PNG) for presentations:

| File | Topic |
|------|--------|
| `system-architecture.svg` | SPA ↔ Elmahdi app ↔ ERPNext |
| `auth-capability-flow.svg` | Session identity → capabilities → routes |
| `purchasing-flow.svg` | Supplier → PR → PI → matching |

When adding assets, keep filenames stable and update README links.
