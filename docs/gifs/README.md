# Demo GIFs

Short loops (10–20s, ≤5 MB) for README workflow sections.

| File | Flow |
|------|------|
| `login-flow.gif` | Sign in → role-based home redirect |
| `pos-checkout.gif` | Scan item → pay → receipt |
| `shift-open-close.gif` | Open shift → sell → close with cash count |
| `returns-approval.gif` | Create return → manager approval (if enabled) |

Capture at 1280×720. Prefer staging data.
